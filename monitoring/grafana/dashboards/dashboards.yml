# Almost exactly the same as the one in the exercise example for session6
apiVersion: 1

providers:
  - name: 'main-dashboard'
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards